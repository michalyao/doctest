get:
  tags:
    - dashboards
  description: '通过Id返回仪表盘详情'
  operationId: getDashboardById
  parameters:
    - name: userId
      in: query
      type: string
      description: '用户Id'
    - name: dashboardid
      in: query
      type: string
      description: '仪表盘Id'
  responses:
    200:
      description: '返回仪表盘'
      schema:
        $ref: '#/definitions/MineDashboard'
  
