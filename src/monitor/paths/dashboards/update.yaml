post:
  tags:
    - dashboards 
  description: '更新仪表盘'
  operationId: updateDashboard 
  parameters:
    - name: tenantId
      in: query
      type: string
      description: '租户Id'
    - name: dashboard
      in: body
      description: '入参'
      schema: 
        $ref: '#/definitions/Dashboard'
  responses:
    200:
      description: '更新成功'
      schema: 
        $ref: '#/definitions/Dashboard'
