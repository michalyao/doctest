get:
  tags: 
    - reference
  description: '获取指标参考'
  operationId: getMetricRefByName
  parameters:
    - name: name
      in: query
      type: string
      description: '指标名称'
  responses:
    200:
      description: '返回指标参考'
      schema:
        $ref: '#/definitions/MetricReference'
  